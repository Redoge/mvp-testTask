package app.redoge.mvp.enums;

// Enum to represent the different game names
public enum GameNames {
    // Enum constant for basketball game
    BASKETBALL,
    // Enum constant for handball game
    HANDBALL
}
